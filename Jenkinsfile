pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building'
                // Add yout Windows build command here
                bat 'echo Running build commands'
            }
        }
    }
}